{
  "name": "lucidauth-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "build:lib": "pnpm --filter lucidauth build",
    "test": "pnpm --filter lucidauth test",
    "test:watch": "pnpm --filter lucidauth test -- --watch",
    "dev:example": "pnpm --filter nextjs-example dev"
  },
  "packageManager": "pnpm@9.15.1"
}
